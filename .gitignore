/bin/
/dist/
.local-run/
.remote-run/
*.log
*.bak
*.bak.*
._*
.DS_Store

# Local configuration and credentials must never be committed.
.env
.env.*
!.env.example
*.pem
*.key
*.p12
*.pfx
*.jks
credentials.json
secrets.json

# 伺服器私鑰與本機 TLS 身分不得提交。
/data/

# 本機 Python 快取與連線設定。
__pycache__/
*.pyc
client-secret.json
sites.json

# 本機建置與封裝入口，不上傳至 Git。
/build.command
/buildMac.command
/buildWin.command
/buildLinux.command
/pack.command

# 本機無終端機啟動入口（由腳本產生）。
/clientUI.app/

# 本機研究產物及直接 go build 輸出。
/reports/
/try/
/remote
/remote.exe
/client.exe

# Android 原始碼已追蹤；僅忽略本機工具、編譯輸出及裝置設定。
/android/.gradle/
/android/.kotlin/
/android/**/build/
/android/local.properties
/android/libs/.*
/android/libs/*.aar
/android/libs/*-sources.jar
/androidcore/
/client
# Release 附加更新重點：只在本機封裝時提供，不提交原始碼。
/release_*.note
