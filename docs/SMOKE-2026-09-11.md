# 發行前 Smoke Test（2026-09-11）

本次可在本機完成的項目均通過，未發現阻擋建置或已測流程的問題。這不代表所有平台與實際網路環境都已驗證。

## 本次結果

| 項目 | 結果與範圍 |
| --- | --- |
| Go 測試 | `go test ./internal/... ./cmd/remote/...` 通過；需環境變數啟用的測試另行執行 |
| Host 實際執行檔 | 私有管道傳入測試密碼，TLS 配對與 Join／Offer 簽章驗證通過；未建立遠端桌面 |
| 剪貼簿 | 同機兩端 P2P 文字及 2.8 MB 檔案雙向傳輸、內容比對通過；使用原生剪貼簿，測試後依既有規則復原 |
| Tailcat | 實際建立通道，雙向 64、1,400、60,000 bytes 封包與分片重組比對通過；未模擬企業防火牆 |
| 自動配置後端 | 4.9、15、29.8 FPS 樣本對應建議範圍 5～30；30 FPS 探測不受舊 10 FPS 偏好限制；預覽不落盤，手動套用與取消通過 |
| 等待套用 | 超過取樣期限後仍使用已顯示的建議；保留等待期間其他偏好變更，避免覆蓋無關設定 |
| 自動配置前端 | 執行實際 JavaScript 控制流程，使用模擬 DOM／API；開啟不取樣、手動開始、預覽不套用、手動套用、關閉及取消復原均通過 |
| MCP | 本機實際入口 initialize／tools/list 成功；白名單內免 Token、名單外拒絕、停用白名單後需 Token 的規則測試通過 |
| Mac | Client／Remote 測試套件與先前修改後的執行檔編譯通過；連結器僅提示重複 `-lobjc` |
| Windows | amd64、arm64 Client／Remote 交叉編譯通過；未在 Windows 執行 |
| WinPE | 最新共用流程編譯、ZIP 封裝與 CRC 檢查通過；含 EXE、啟動腳本、說明、雜湊與授權文件 |
| 語法 | Client JavaScript、翻譯 JSON、release.py 語法通過 |

## 同日已完成的正式服務測試

HTTPS 8081 的 TLS 1.3／憑證與健康檢查、HTTPS 雙向訊號、WSS／HTTPS 混合配對、WSS 失敗自動備援、46 秒待機心跳及工作階段關閉均已通過。此輪未修改 HTTPS 實作，未重複建立正式服務測試房間。詳見 [HTTPS 訊號備援](HTTPS-SIGNALING.md)。

## 發行前需保留的限制

- 原生視窗顯示時機、焦點與視覺排列尚未做實際點選驗證；前端流程測試使用模擬 DOM。
- 本機剪貼簿 loopback 不等於兩台 Mac 的 Finder 手動複製貼上全流程；圖片未在本輪另測。
- Windows 安裝／自動更新、未登入連線與 WinPE 開機尚未實機驗證。
- Tailcat 完成通道層測試，尚未在受限網路完成完整遠端畫面與單端切換矩陣。維持實驗性標示。
- MCP 本輪只驗證本機連通與存取規則；未執行遠端操作、資料讀取或 Shell。尚未公開的能力不納入 Release 說明。
- Server 首次 Offer 延遲超過 45 秒可能短暫顯示不可用，已有記錄；正常先送 Offer 後待機的流程通過。

## 可重跑入口

```sh
go test ./internal/... ./cmd/remote/...
go test ./internal/clientui -run 'Test(AutoStream|MCPWhitelist)ReleaseSmoke' -count=1 -v
node scripts/smoke-client-ui.cjs
YOURDESK_CLIPBOARD_SMOKE=1 go test ./internal/clipboard -run TestClipboardLoopbackSmoke -count=1 -v
YOURDESK_TAILCAT_SMOKE=1 go test ./internal/peertransport -run TestTailcatLiveSmoke -count=1 -v -timeout 90s
./scripts/build-winpe.sh
```

Host 執行檔測試另以 `YOURDESK_HOST_SMOKE_BINARY` 指向已建置的 Client，執行 `TestHostBinaryAuthenticationSmoke`。測試使用獨立房間與臨時檔案，不操作使用者的遠端桌面。

已於本日發布 1.26.0911 build 1733。發行工作目錄排除未公開的新 MCP／遠端指令後，Go 測試與前端流程測試再次通過；Mac App／DMG 已簽章公證，Windows Installer 與 WinPE ZIP 已產生。GitHub 五個附件的 SHA-256 與本機一致。
